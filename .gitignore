# Local build / distribution dirs
build/
dist/
*.spec.bak

# Stray Python bytecode (repo is Electron-only; ignore if present locally)
__pycache__/
*.py[cod]

# Virtual environments (optional tooling)
.venv/
.venv-*/
venv/
ENV/

# Type / lint caches
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
*.swp
*~

# Large redistributable (obtain from your game/driver install if needed)
amdxcffx64.dll

# Electron app — local temp / scratch
app/temp/
